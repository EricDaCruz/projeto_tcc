import styled from 'styled-components'

export const Input = styled.input` 
    border: none;
    background-color: transparent;
    outline: none;
    flex: 1;
    height: 3rem;

   &::placeholder{
       color: white;
       weight: 600
   }
`